/* eslint-disable react/prop-types */
import React from "react";
import { Menu } from "@mui/material";
const FileMenu = ({anchorE1}) => {
  return <Menu open={false} anchorEl={anchorE1}>Hello</Menu>;
};

export default FileMenu;
